generator client {
  provider        = "prisma-client"
  output          = "../src/database/prisma-client"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider = "postgresql"

  extensions = [pgcrypto]
}
